This project compares four popular classification algorithms: Logistic Regression, Support Vector Machine (SVM), Random Forest (RF), and K-Nearest Neighbors (KNN). Each model is trained on the same dataset and evaluated using accuracy, precision, recall, and F1-score.
